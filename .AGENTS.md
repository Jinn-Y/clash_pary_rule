# Clash Party Rule 项目 AI Agent 指南

## 项目概述

这是一个用于 Clash / Clash.Meta / Mihomo 的 JavaScript 配置覆写脚本项目。它通过拦截和修改原始订阅配置，自动生成更适合日常使用的规则、策略组、DNS 配置和链式代理能力。

### 核心价值
- **自动化配置优化**：将杂乱的机场订阅转换为有序的策略组
- **智能分流**：基于服务类型自动分配流量到不同代理策略
- **地区识别**：自动从节点名称提取地区信息并生成地区组
- **链式代理**：支持二级代理链路（入口节点 → 出口节点）

## 技术架构

### 技术栈
- **语言**：JavaScript (ES6+)
- **运行环境**：Clash Meta / Mihomo 内核的 JavaScript 引擎
- **核心接口**：`main(config)` 函数
- **依赖**：无外部依赖，纯 JavaScript 实现

### 项目结构
```
clash_pary_rule/
├── clash_party_rule.js  # 主脚本文件（核心逻辑）
├── README.md            # 用户文档
└── .AGENTS.md          # AI Agent 指南（本文件）
```

## 核心功能模块

### 1. 规则分流模块
**位置**：`clash_party_rule.js` 中的 `rules` 数组

**功能**：定义流量分流规则，将不同类型的流量送入对应策略组

**关键规则类别**：
- AI 服务（OpenAI、Claude、Gemini、Perplexity 等）
- Google 服务（搜索、YouTube、Gmail 等）
- Github / Gitlab
- 流媒体（Netflix、Disney+、Spotify 等）
- 游戏平台（Steam、Epic、PlayStation 等）
- 金融服务（银行、支付、交易所等）
- 社交媒体（Twitter、Facebook、Instagram 等）
- 国内服务（微信、支付宝、淘宝等）
- 广告拦截

### 2. 地区节点识别模块
**位置**：`clash_party_rule.js` 中的 `regionKeywords` 对象

**功能**：通过正则表达式从节点名称中识别地区信息

**支持地区**：
- 香港、台湾、日本、美国、新加坡
- 韩国、英国、德国、加拿大、澳大利亚
- 法国、印度、土耳其、阿根廷、俄罗斯、荷兰

### 3. 策略组生成模块
**位置**：`clash_party_rule.js` 中的 `proxy-groups` 生成逻辑

**生成的策略组类型**：
- **主策略组**：`🚀 节点选择`、`⚡ 自动选择`、`🔄 故障切换`
- **业务策略组**：AI 服务、谷歌服务、Github、流媒体等
- **地区策略组**：根据识别到的节点自动生成
- **链式代理组**：入口节点、出口节点、链式代理

### 4. DNS 配置模块
**位置**：`clash_party_rule.js` 中的 `dns` 配置对象

**特性**：
- 启用 `fake-ip` 模式
- 国内外 DNS 分流
- 支持 DoH (DNS over HTTPS)
- 针对特定域名的 DNS 优化

### 5. 链式代理模块
**位置**：节点复制和 `dialer-proxy` 配置逻辑

**工作原理**：
1. 复制原始节点为入口节点（添加 `↗️` 前缀）
2. 复制原始节点为出口节点（添加 `↘️` 前缀）
3. 出口节点通过 `dialer-proxy` 指向入口节点组
4. 形成二级代理链路

## 开发指南

### 修改规则
1. **添加新规则**：在 `rules` 数组中添加新条目
   ```javascript
   ["DOMAIN-SUFFIX", "example.com", "🚀 节点选择"],
   ```

2. **修改规则优先级**：调整规则在数组中的顺序（前面的规则优先级更高）

3. **添加新规则集**：在 `ruleProviders` 对象中添加
   ```javascript
   "new-provider": {
     type: "http",
     url: "规则集URL",
     interval: 86400,
     format: "yaml"
   }
   ```

### 添加新地区
在 `regionKeywords` 对象中添加新的地区识别规则：
```javascript
"地区名": /正则表达式/i,
```

### 自定义策略组
1. **修改业务组**：编辑 `ruleGroupNames` 对象
2. **修改默认策略**：编辑 `ruleGroupDefaults` 对象
3. **调整策略组内容**：修改策略组生成逻辑中的 `proxies` 数组

### 调试技巧
1. **检查节点识别**：查看生成的地区组是否正确
2. **验证规则匹配**：使用 Clash 的日志功能查看流量匹配情况
3. **测试链式代理**：选择入口和出口节点，验证连接是否正常

## 常见问题处理

### Q1: 节点没有被正确分组到地区
**解决**：检查节点名称是否包含地区关键词，必要时修改 `regionKeywords` 正则

### Q2: 某个服务没有走正确的代理
**解决**：
1. 检查规则顺序（可能被前面的规则匹配）
2. 添加更精确的规则
3. 检查规则集是否包含该域名

### Q3: 链式代理不工作
**解决**：
1. 确认 Clash 内核支持 `dialer-proxy`
2. 检查入口和出口节点是否正常
3. 验证节点复制逻辑是否正确执行

### Q4: DNS 解析问题
**解决**：
1. 检查 DNS 服务器是否可访问
2. 验证 `fake-ip-filter` 配置
3. 查看 DNS 日志排查具体域名问题

## 性能优化建议

1. **减少节点数量**：链式代理会复制节点，节点过多会影响性能
2. **优化正则表达式**：使用更精确的正则减少匹配开销
3. **合理设置规则集更新间隔**：避免过于频繁的更新
4. **精简规则**：删除不需要的规则，减少匹配次数

## 安全注意事项

1. **规则来源**：只使用可信的规则集来源
2. **节点安全**：确保节点来源可靠
3. **隐私保护**：注意 DNS 查询可能泄露浏览记录
4. **配置备份**：修改前备份原始配置

## 版本兼容性

### 支持的 Clash 内核
- Clash Meta (推荐)
- Mihomo
- 其他支持 JavaScript 覆写的 Clash 分支

### 最低要求
- 支持 `main(config)` 函数接口
- 支持 `dialer-proxy`（链式代理功能）
- 支持 `rule-providers`
- 支持 JavaScript ES6 语法

## 贡献指南

### 提交规范
使用约定式提交（Conventional Commits）：
- `feat:` 新功能
- `fix:` 修复问题
- `docs:` 文档更新
- `refactor:` 代码重构
- `perf:` 性能优化

### 代码风格
- 使用 2 空格缩进
- 保持代码简洁清晰
- 添加必要的注释（中文）
- 遵循现有代码风格

### 测试要求
- 修改后在实际 Clash 客户端测试
- 验证规则匹配是否正确
- 确认策略组生成无误
- 测试链式代理功能

## 维护建议

### 定期更新
1. **规则集 URL**：检查上游规则集是否有更新
2. **新服务规则**：添加新出现的服务规则
3. **地区关键词**：根据节点命名趋势更新

### 监控要点
- 规则集下载是否正常
- 节点识别准确率
- 用户反馈的分流问题
- 性能影响

### 文档维护
- 保持 README.md 与代码同步
- 记录重要变更
- 更新使用示例

## 联系与支持

如需技术支持或有改进建议，请通过以下方式：
1. 提交 GitHub Issue
2. 发起 Pull Request
3. 参考项目 README.md 中的联系方式

---

*本文档为 AI Agent 理解和维护项目而设计，请保持更新。*
*最后更新：2024年6月*